<!-- What changed and why. Link the issue if there is one. -->

- [ ] `bun run typecheck` and `bun test` pass
- [ ] SPEC.md updated if behaviour, the API or the data model changed
- [ ] Checked the UI at phone width (if it touches the UI)
